pretrained_model_name_or_path = "path/to/sdxl_base.safetensors"
teacher_lora_weights = "path/to/teacher.safetensors"
student_init_weights = "path/to/teacher.safetensors"
cache_manifest = "outputs/self_distill_cache/manifest.jsonl"
prompt_bank = "outputs/self_distill/prompt_bank.json"
output_dir = "outputs/self_distill_train"
output_name = "self_distill_v2"
network_module = "networks.lora"
dim_from_weights = true
network_alpha = 1.0
resolution = 640
train_batch_size = 1
gradient_accumulation_steps = 1
max_train_steps = 1500
learning_rate = 2.0e-5
unet_lr = 2.0e-5
optimizer_preset = "adamw8bit"
lr_scheduler = "constant"
mixed_precision = "fp16"
save_precision = "fp16"
gradient_checkpointing = true
max_data_loader_n_workers = 1
prediction_target = "eps"
attention_backend = "auto"
xt_source_mode = "teacher_rollout"
export_te_mode = "preserve"
num_target_timesteps = 4
timestep_sampling_mode = "uniform"
variant_quota = { keep_strong = 0.35, keep_weak = 0.20, off_null = 0.25, frontier = 0.10, suppress_trigger_style_b = 0.10 }
use_keep_delta_loss = true
use_suppress_to_base_loss = true
use_weight_anchor_loss = true
use_coarse_preservation_loss = false
use_high_pass_delta_loss = false
use_low_pass_delta_loss = false
use_sparse_loss = false
keep_delta_loss_weight = 1.0
suppress_to_base_loss_weight = 1.0
weight_anchor_loss_weight = 0.05
coarse_preservation_loss_weight = 0.0
high_pass_delta_loss_weight = 0.0
low_pass_delta_loss_weight = 0.0
sparse_loss_weight = 0.0
coarse_target_mode = "teacher"
high_pass_mode = "dog"
low_pass_mode = "avg"
per_variant_loss_weight = { keep_strong = 1.0, keep_weak = 0.8, off_null = 1.0, frontier = 0.8 }
per_block_anchor_weight = { input = 1.0, mid = 1.0, output = 1.0, conv = 1.0, default = 1.0 }
